export interface PageTextProps {
  main: {
    topText: string;
    underText: string;
  };
  mainSecond: {
    topText: string;
    underText: string;
  };
  favorites: {
    topText: string;
    underText: string;
  };
}
